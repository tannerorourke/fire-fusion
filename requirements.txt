# --- Core
# Default PyPI wheel for torch 2.2.2 is CUDA 12.1, no extra index needed. A different CUDA build installs over the top:
#   pip install torch==2.2.2 --index-url https://download.pytorch.org/whl/cu118
# Naming that index here instead would break uv, which resolves each package
# from the first index that offers it and would take stale pins from it.
torch==2.2.2
numpy>=1.26,<1.27
pandas>=2.2,<2.3
scipy>=1.12,<1.13
scikit-learn>=1.4
tqdm>=4.66

# --- Geospatial
# These wheels bundle their own GDAL/PROJ/GEOS, no system libraries are needed.
# Bundled GDAL has no HDF4 driver. MODIS HDF-EOS2 granules are read through pyhdf instead and georeferenced from their own grid metadata.
rasterio>=1.4.3,<1.5
pyhdf>=0.11
rioxarray>=0.15,<0.16
geopandas>=0.14,<0.15
shapely>=2.0,<2.1
pyproj>=3.6,<3.7
pyogrio>=0.7
fiona>=1.9

# --- Array IO (netcdf4, h5netcdf and zarr are xarray engines, imported indirectly)
xarray>=2024.3,<2024.4
zarr>=2,<3
numcodecs>=0.12,<0.16
dask>=2024.1
netcdf4>=1.6
h5netcdf>=1.3

# --- Source ingest
pyarrow>=15
earthaccess>=0.11
requests>=2.31
s3fs>=2024.2
boto3>=1.34

# --- Visualization / telemetry
matplotlib>=3.8
tensorboard>=2.16

# --- Package (editable; run from FireFusionNet/)
-e .
